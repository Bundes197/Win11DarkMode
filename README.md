# Win11DarkMode
Script, that enables dark mode across the whole OS despite not having active Windows 11 license.

## Usage
Simply open run.bat and continue according to the terminal.
If an error occurs, please try running run.bat as administrator.

### Parameters
You can use parameters in cmd to change program behavior, only first parameter is accepted
/r = change to dark mode (default)
/l = change to light mode
